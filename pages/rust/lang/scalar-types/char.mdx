# 字符类型

所有`Unicode`值都可以作为 Rust 字符，字符类型占用的内存大小为`4`个字节

字符类型用单引号包裹

```rs
let c: char = 'z';
let z = 'ℤ';
let g = '国';
let heart_eyed_cat = '😻';
```
