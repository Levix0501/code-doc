# 数据类型

Rust 中的每个值都有特定的类型，数据类型分为`基本类型`和`复合类型`

Rust 是一门静态类型语言，编译器必须在编译阶段就知道所有变量的类型

## 类型推导

Rust 编译器可以根据变量值和上下文使用方式来自动推导变量的类型

但在某些复杂的情况下，编译器无法推导出变量类型，需要手动标注类型

## 类型标注

声明变量时可以给变量添加`类型标注`，如：

```rs
let a: bool = true; // let 变量名: 变量类型 = 变量值;
```
